APP_ENV=docker
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=appdb
DATABASE_URL=postgresql+psycopg://app:app@db:5432/appdb
OTHER_API_BASE=http://other-api:8000


SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=docu.serve.emailer@gmail.com
SMTP_PASSWORD=     ## Need to insert password here
SMTP_FROM=Docu-Serve <docu.serve.emailer@gmail.com>
SMTP_USE_SSL=true
SMTP_STARTTLS=false

# RabbitMQ connection for receiving user events from other services
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
